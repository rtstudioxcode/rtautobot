// BottomNav is no longer used — mobile navigation is handled by the subnav chips in Topbar.
// This component is kept as a stub to avoid import errors in case it's referenced elsewhere.

export default function BottomNav() {
  return null;
}
