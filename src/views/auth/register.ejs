
<% const title = "สมัครสมาชิก"; %>

<section class="rtx-auth rtx-register" aria-label="สมัครสมาชิก RTAUTOBOT">
  <div class="rtx-bg" aria-hidden="true">
    <span class="orb orb-a"></span>
    <span class="orb orb-b"></span>
    <span class="orb orb-c"></span>
  </div>

  <div class="rtx-auth-grid">
    <aside class="rtx-hero-panel">
      <div class="rtx-kicker"><span>✦</span> RTAUTOBOT REGISTER MEMBER</div>
      <h1>สมัครสมาชิกเพื่อเริ่มใช้บริการ Social แบบมืออาชีพ</h1>
      <p>
        เปิดบัญชีเดียวเพื่อสั่งซื้อบริการ เพิ่มผู้ติดตาม ไลค์ วิว เอนเกจเมนต์ เติมเครดิต
        และติดตามงานทั้งหมดได้แบบปลอดภัย
      </p>

      <div class="rtx-benefits">
        <div class="rtx-benefit">
          <span class="rtx-benefit-icon">⚡</span>
          <strong>เริ่มใช้งานไว</strong>
          <small>สมัครง่ายไม่กี่ขั้นตอน พร้อมใช้งานได้ทันทีทุกเวลา</small>
        </div>
        <div class="rtx-benefit">
          <span class="rtx-benefit-icon">🛡️</span>
          <strong>ปลอดภัยกว่า</strong>
          <small>ยืนยันอีเมล รองรับระบบ OTP และ Session</small>
        </div>
        <div class="rtx-benefit">
          <span class="rtx-benefit-icon">📈</span>
          <strong>ครบในที่เดียว</strong>
          <small>เติมเครดิตออโต้ สั่งออเดอร์ รายงานสถานะ และบริการหลากหลายแพลตฟอร์ม</small>
        </div>
      </div>

      <div class="rtx-meta">
        <span>All Right Reserved</span>
        <i></i>
        <span>© 2026</span>
        <i></i>
        <span>RTAUTOBOT</span>
      </div>
    </aside>

    <main class="rtx-form-panel">
      <div class="rtx-form-head">
        <span class="rtx-lock">✨</span>
        <div>
          <h2>สร้างบัญชีใหม่</h2>
          <p>กรอกข้อมูลให้ครบ เพื่อเปิดใช้งานบัญชี RTAUTOBOT</p>
        </div>
      </div>

      <% if (typeof error === 'string' && error) { %>
        <div class="rtx-alert error"><%= error %></div>
      <% } %>

      <form id="registerForm" class="rtx-form" action="/register" method="post" novalidate>
        <input type="hidden" name="next" value="<%= typeof next === 'string' ? next : '/' %>">
        <input type="hidden" name="aff" value="<%= (typeof aff==='string' ? aff : '') %>">
        <input type="hidden" name="affiliateKey" value="<%= (typeof aff==='string' ? aff : '') %>">

        <label class="rtx-field">
          <span class="rtx-label">ชื่อผู้ใช้</span>
          <div class="rtx-input-wrap">
            <span class="rtx-input-icon">👤</span>
            <input class="rtx-input" name="username" required autocomplete="username" placeholder="เช่น rtssm-th">
          </div>
          <small>ใช้อักษร a–z, 0–9 และขีดกลางได้</small>
        </label>

        <label class="rtx-field">
          <span class="rtx-label">อีเมล</span>
          <div class="rtx-input-wrap">
            <span class="rtx-input-icon">✉️</span>
            <input class="rtx-input" type="email" name="email" required placeholder="you@email.com" autocomplete="email">
          </div>
          <small>ใช้สำหรับยืนยันตัวตนและกู้รหัสผ่าน</small>
        </label>

        <label class="rtx-field">
          <span class="rtx-label">ชื่อ–นามสกุล</span>
          <div class="rtx-input-wrap">
            <span class="rtx-input-icon">🪪</span>
            <input class="rtx-input" name="name" required autocomplete="name" placeholder="ชื่อจริง นามสกุล">
          </div>
          <small>แนะนำให้ใช้ชื่อจริงเพื่อความปลอดภัยของบัญชี</small>
        </label>

        <label class="rtx-field">
          <span class="rtx-label">รหัสผ่าน</span>
          <div class="rtx-input-wrap rtx-password-wrap">
            <span class="rtx-input-icon">••</span>
            <input class="rtx-input" type="password" name="password" required autocomplete="new-password" minlength="6" placeholder="อย่างน้อย 6 ตัวอักษร" id="pwd">
            <button type="button" class="rtx-pass" id="togglePwd" aria-pressed="false" aria-controls="pwd" aria-label="แสดงหรือซ่อนรหัสผ่าน">
              <svg class="eye eye-open" viewBox="0 0 24 24" width="19" height="19" aria-hidden="true">
                <path d="M2.5 12s3.5-6.5 9.5-6.5S21.5 12 21.5 12s-3.5 6.5-9.5 6.5S2.5 12 2.5 12Z" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"/>
                <circle cx="12" cy="12" r="2.7" fill="none" stroke="currentColor" stroke-width="1.9"/>
              </svg>
              <svg class="eye eye-off" viewBox="0 0 24 24" width="19" height="19" aria-hidden="true">
                <path d="M3 3l18 18" fill="none" stroke="currentColor" stroke-width="2.1" stroke-linecap="round"/>
                <path d="M10.6 5.7A9.9 9.9 0 0 1 12 5.5c6 0 9.5 6.5 9.5 6.5a18 18 0 0 1-2.3 3.2M6.7 6.9C4 8.7 2.5 12 2.5 12s3.5 6.5 9.5 6.5c1.8 0 3.4-.6 4.7-1.4" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"/>
              </svg>
            </button>
          </div>
          <small>ตั้งรหัสอย่างน้อย 6 ตัวอักษร ยิ่งยาวยิ่งปลอดภัย</small>
        </label>

        <button class="rtx-submit" type="submit" id="btnRegister">
          <span>สมัครสมาชิก</span>
          <b>→</b>
        </button>

        <div class="rtx-form-foot">
          <span>มีบัญชีอยู่แล้ว?</span>
          <a href="/login">เข้าสู่ระบบ</a>
        </div>
      </form>
    </main>
  </div>
</section>

<dialog id="msgDialog" class="rtx-dialog" data-variant="info">
  <div class="rtx-dialog-card" role="alertdialog" aria-labelledby="msgTitle" aria-describedby="msgText">
    <div class="rtx-dialog-head">
      <div class="rtx-dialog-badge" id="msgBadge" aria-hidden="true">?</div>
      <div>
        <h3 id="msgTitle">หัวข้อ</h3>
        <p>RTAUTOBOT Notification</p>
      </div>
    </div>
    <div class="rtx-dialog-body">
      <p id="msgText">รายละเอียดข้อความ</p>
    </div>
    <div class="rtx-dialog-actions">
      <button class="rtx-mini ghost" id="msgCancel" hidden>ยกเลิก</button>
      <button class="rtx-mini primary" id="msgClose">ตกลง</button>
    </div>
  </div>
</dialog>

<dialog id="registerNoticeModal" class="rtx-dialog rtx-notice rtx-enterprise-notice" aria-labelledby="regNoticeTitle" aria-describedby="regNoticeText">
  <div class="rtx-notice-card" role="alertdialog">
    <div class="rtx-notice-aura" aria-hidden="true"></div>

    <div class="rtx-notice-head">
      <div class="rtx-notice-badge" aria-hidden="true">
        <span>⚠️</span>
      </div>
      <div class="rtx-notice-titlebox">
        <span class="rtx-notice-kicker">REGISTER SECURITY NOTICE</span>
        <h3 id="regNoticeTitle">แจ้งเตือนก่อนสมัคร</h3>
        <p id="regNoticeText">โปรดตรวจสอบข้อมูลให้ถูกต้องก่อนสร้างบัญชี</p>
      </div>
    </div>

    <div class="rtx-notice-body">
      <div class="rtx-notice-list" aria-label="ข้อควรทราบก่อนสมัคร">
        <div class="rtx-notice-item">
          <span class="rtx-notice-no">01</span>
          <div>
            <strong>ใช้ข้อมูลจริงตามกฎหมาย</strong>
            <small>ข้อมูลบัญชีควรตรงกับตัวตนจริง เพื่อความปลอดภัยและการตรวจสอบในอนาคต</small>
          </div>
        </div>

        <div class="rtx-notice-item">
          <span class="rtx-notice-no">02</span>
          <div>
            <strong>อีเมลต้องยืนยันได้จริง</strong>
            <small>ระบบจะใช้สำหรับ OTP, แจ้งเตือน, กู้รหัสผ่าน และความปลอดภัยของบัญชี</small>
          </div>
        </div>

        <div class="rtx-notice-item warn">
          <span class="rtx-notice-no">03</span>
          <div>
            <strong>ชื่อ–นามสกุลควรตรงกับเอกสารจริง</strong>
            <small>กรอกข้อมูลให้ครบและถูกต้อง เพราะมีผลต่อความปลอดภัยของบัญชี</small>
          </div>
        </div>
      </div>

      <div class="rtx-notice-warning">
        <span aria-hidden="true">✦</span>
        <p>Notice นี้จะแสดงทุกครั้งที่เข้าหน้าสมัคร เพื่อให้ผู้ใช้ตรวจสอบข้อมูลก่อนสมัครสมาชิกเสมอ</p>
      </div>
    </div>

    <div class="rtx-notice-actions">
      <button class="rtx-notice-accept" id="regNoticeClose" type="button">
        <span class="shine" aria-hidden="true"></span>
        <span>ปิดและยอมรับ</span>
        <b aria-hidden="true">→</b>
      </button>
    </div>
  </div>
</dialog>

<dialog id="otpDialog" class="rtx-dialog rtx-otp">
  <div class="rtx-dialog-card" role="dialog" aria-labelledby="otpTitle">
    <div class="rtx-dialog-head">
      <div class="rtx-dialog-badge" aria-hidden="true">🔐</div>
      <div>
        <h3 id="otpTitle">ยืนยันอีเมล</h3>
        <p id="otpHelp">เราได้ส่งรหัส 6 หลักไปที่อีเมลของคุณแล้ว</p>
      </div>
    </div>
    <div class="rtx-dialog-body">
      <label class="rtx-field otp-field">
        <span class="rtx-label">รหัส OTP</span>
        <input id="otpCode" class="rtx-input" inputmode="numeric" pattern="[0-9]*" maxlength="6" placeholder="______">
      </label>
      <div class="rtx-otp-tools">
        <span id="otpTimer">ส่งใหม่ได้ใน 60s</span>
        <button id="btnResend" class="rtx-link-btn" disabled type="button">ส่งใหม่</button>
      </div>
    </div>
    <div class="rtx-dialog-actions">
      <button class="rtx-mini ghost" id="btnCancelOtp" type="button">ยกเลิก</button>
      <button class="rtx-mini primary" id="btnVerifyOtp" type="button">ยืนยัน</button>
    </div>
  </div>
</dialog>

<style>
  .rtx-auth{
    --rtx-page:var(--page,#08090b);
    --rtx-card:var(--card,#17181d);
    --rtx-card2:color-mix(in srgb,var(--card,#17181d) 88%,#000 12%);
    --rtx-soft:color-mix(in srgb,var(--card,#17181d) 72%,var(--page,#08090b));
    --rtx-text:var(--text,#fff6e3);
    --rtx-muted:var(--muted,#d9c99d);
    --rtx-border:var(--border,rgba(255,255,255,.11));
    --rtx-accent:var(--accent,#f7cb3b);
    --rtx-accent2:#d69a22;
    --rtx-danger:#ff5a7a;
    --rtx-green:#31d983;
    min-height:calc(100vh - 88px);
    position:relative;
    isolation:isolate;
    overflow:hidden;
    padding:clamp(18px,3vw,42px);
    color:var(--rtx-text);
    background:
      radial-gradient(circle at 10% 18%,color-mix(in srgb,var(--rtx-accent) 22%,transparent),transparent 31%),
      radial-gradient(circle at 88% 72%,rgba(32,119,255,.24),transparent 36%),
      linear-gradient(135deg,color-mix(in srgb,var(--rtx-page) 96%,#15161c),var(--rtx-page));
  }
  .rtx-bg{position:absolute;inset:0;pointer-events:none;z-index:-1;background-image:linear-gradient(rgba(255,255,255,.03) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,.03) 1px,transparent 1px);background-size:64px 64px;mask-image:radial-gradient(circle at center,#000 0 58%,transparent 84%)}
  .orb{position:absolute;border-radius:999px;filter:blur(38px);opacity:.65;animation:rtxFloat 9s ease-in-out infinite alternate}.orb-a{width:220px;height:220px;left:5%;top:18%;background:color-mix(in srgb,var(--rtx-accent) 54%,transparent)}.orb-b{width:260px;height:260px;right:8%;bottom:10%;background:rgba(49,118,255,.42);animation-delay:-2.2s}.orb-c{width:160px;height:160px;left:45%;bottom:18%;background:rgba(138,92,246,.38);animation-delay:-4s}
  @keyframes rtxFloat{from{transform:translate3d(0,0,0) scale(1)}to{transform:translate3d(18px,-22px,0) scale(1.08)}}
  .rtx-auth-grid{width:min(1240px,100%);margin:0 auto;min-height:calc(100vh - 160px);display:grid;grid-template-columns:minmax(0,1.22fr) minmax(380px,.78fr);align-items:center;gap:clamp(20px,4vw,44px)}
  .rtx-hero-panel,.rtx-form-panel{position:relative;border:1px solid var(--rtx-border);background:linear-gradient(145deg,color-mix(in srgb,var(--rtx-card) 88%,transparent),color-mix(in srgb,var(--rtx-card2) 92%,transparent));box-shadow:0 30px 90px rgba(0,0,0,.36),inset 0 1px 0 rgba(255,255,255,.07);backdrop-filter:blur(18px);overflow:hidden;animation:rtxRise .68s cubic-bezier(.2,.8,.2,1) both}
  .rtx-hero-panel{border-radius:30px;padding:clamp(28px,4vw,54px)}
  .rtx-hero-panel:before,.rtx-form-panel:before{content:"";position:absolute;inset:-1px;background:linear-gradient(115deg,transparent 0 45%,rgba(255,255,255,.12) 46% 54%,transparent 55%);transform:translateX(-55%);animation:rtxSheen 6.5s ease-in-out infinite;pointer-events:none}.rtx-form-panel:before{animation-delay:-1.4s}
  @keyframes rtxSheen{0%,42%{transform:translateX(-60%)}70%,100%{transform:translateX(72%)}}
  @keyframes rtxRise{from{opacity:0;transform:translateY(18px) scale(.985)}to{opacity:1;transform:translateY(0) scale(1)}}
  .rtx-kicker{width:max-content;max-width:100%;display:inline-flex;align-items:center;gap:9px;padding:9px 15px;border-radius:999px;border:1px solid color-mix(in srgb,var(--rtx-accent) 52%,transparent);background:color-mix(in srgb,var(--rtx-accent) 12%,transparent);color:color-mix(in srgb,var(--rtx-accent) 74%,var(--rtx-text));font-size:12px;font-weight:650;letter-spacing:.07em;text-transform:uppercase;box-shadow:0 12px 30px color-mix(in srgb,var(--rtx-accent) 12%,transparent)}
  .rtx-hero-panel h1{position:relative;margin:26px 0 16px;font-size:clamp(42px,5.5vw,72px);line-height:.98;letter-spacing:-.055em;font-weight:650;color:var(--rtx-text);text-wrap:balance}.rtx-hero-panel p{position:relative;margin:0;max-width:720px;color:color-mix(in srgb,var(--rtx-muted) 86%,var(--rtx-text));font-size:clamp(16px,1.45vw,19px);line-height:1.8;font-weight:750}
  .rtx-benefits{position:relative;margin-top:42px;display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:14px}.rtx-benefit{border:1px solid var(--rtx-border);border-radius:22px;padding:17px;background:color-mix(in srgb,var(--rtx-soft) 64%,transparent);transition:transform .22s ease,border-color .22s ease,background .22s ease}.rtx-benefit:hover{transform:translateY(-4px);border-color:color-mix(in srgb,var(--rtx-accent) 42%,var(--rtx-border));background:color-mix(in srgb,var(--rtx-accent) 8%,var(--rtx-soft))}.rtx-benefit-icon{width:44px;height:44px;display:grid;place-items:center;border-radius:15px;background:linear-gradient(135deg,#ffe28a,#d39b2a);box-shadow:0 16px 30px rgba(214,154,34,.22);margin-bottom:13px}.rtx-benefit strong{display:block;font-weight:650;color:var(--rtx-text);margin-bottom:5px}.rtx-benefit small{display:block;color:var(--rtx-muted);line-height:1.45;font-weight:650}
  .rtx-proofline{position:relative;margin-top:38px;display:flex;align-items:center;gap:10px;flex-wrap:wrap;color:color-mix(in srgb,var(--rtx-muted) 78%,var(--rtx-text));font-size:12px;font-weight:600}.rtx-proofline i{width:5px;height:5px;border-radius:50%;background:var(--rtx-accent)}
  .rtx-form-panel{border-radius:28px;padding:clamp(24px,2.4vw,34px);animation-delay:.08s}.rtx-form-head{position:relative;display:flex;align-items:center;gap:16px;margin-bottom:22px}.rtx-lock{width:54px;height:54px;border-radius:18px;display:grid;place-items:center;background:linear-gradient(135deg,#ffe28a,#d39b2a);box-shadow:0 18px 36px rgba(214,154,34,.22)}.rtx-form-head h2{margin:0;font-size:clamp(28px,2.7vw,38px);font-weight:950;letter-spacing:-.035em}.rtx-form-head p{margin:2px 0 0;color:var(--rtx-muted);font-weight:800}
  .rtx-alert{padding:13px 15px;border-radius:16px;margin-bottom:16px;border:1px solid color-mix(in srgb,var(--rtx-danger) 45%,transparent);background:color-mix(in srgb,var(--rtx-danger) 11%,transparent);color:#ffd4dc;font-weight:550}
  .rtx-form{display:grid;gap:15px}.rtx-field{display:grid;gap:8px}.rtx-label{font-weight:650;color:color-mix(in srgb,var(--rtx-muted) 84%,var(--rtx-text));font-size:13px}.rtx-field small{color:color-mix(in srgb,var(--rtx-muted) 78%,transparent);font-weight:650;line-height:1.35}.rtx-input-wrap{min-height:54px;display:grid;grid-template-columns:56px minmax(0,1fr);align-items:center;border:1px solid color-mix(in srgb,var(--rtx-border) 92%,transparent);border-radius:18px;background:color-mix(in srgb,var(--rtx-soft) 68%,transparent);overflow:hidden;transition:border-color .18s ease,box-shadow .18s ease,background .18s ease}.rtx-input-wrap:focus-within{border-color:color-mix(in srgb,var(--rtx-accent) 72%,transparent);box-shadow:0 0 0 4px color-mix(in srgb,var(--rtx-accent) 14%,transparent),0 16px 32px rgba(0,0,0,.18);background:color-mix(in srgb,var(--rtx-soft) 86%,transparent)}.rtx-input-icon{height:100%;display:grid;place-items:center;background:rgba(0,0,0,.34);color:var(--rtx-accent);font-weight:950}.rtx-input{width:100%;height:100%;border:0;outline:0;background:transparent;color:var(--rtx-text);font:inherit;font-weight:850;padding:0 16px}.rtx-input::placeholder{color:color-mix(in srgb,var(--rtx-muted) 48%,transparent)}
  .rtx-password-wrap{grid-template-columns:56px minmax(0,1fr) 58px}.rtx-pass{width:44px;height:44px;border:1px solid color-mix(in srgb,var(--rtx-border) 85%,transparent);border-radius:15px;background:color-mix(in srgb,var(--rtx-soft) 58%,transparent);color:var(--rtx-text);cursor:pointer;display:grid;place-items:center;justify-self:center;transition:transform .18s ease,border-color .18s ease,background .18s ease}.rtx-pass:hover{transform:translateY(-1px);border-color:color-mix(in srgb,var(--rtx-accent) 60%,transparent);background:color-mix(in srgb,var(--rtx-accent) 12%,var(--rtx-soft))}.rtx-pass .eye-off{display:none}.rtx-pass.is-visible .eye-open{display:none}.rtx-pass.is-visible .eye-off{display:block}
  .rtx-submit{margin-top:8px;min-height:58px;border:0;border-radius:19px;background:linear-gradient(135deg,#ffe28a,#e8bc53 55%,#bd8626);color:#17130a;font-weight:650;font-size:16px;display:flex;align-items:center;justify-content:center;gap:14px;cursor:pointer;box-shadow:0 22px 42px color-mix(in srgb,var(--rtx-accent) 24%,transparent);transition:transform .2s ease,filter .2s ease,box-shadow .2s ease}.rtx-submit:hover{transform:translateY(-2px);filter:saturate(1.05);box-shadow:0 26px 48px color-mix(in srgb,var(--rtx-accent) 32%,transparent)}.rtx-submit:disabled{opacity:.65;cursor:wait;transform:none}.rtx-submit b{font-size:20px}.rtx-form-foot{display:flex;align-items:center;justify-content:space-between;gap:12px;color:var(--rtx-muted);font-weight:550;flex-wrap:wrap}.rtx-form-foot a{color:var(--rtx-accent);text-decoration:none;font-weight:650}.rtx-form-foot a:hover{text-decoration:underline}
  .rtx-dialog{border:0;padding:0;background:transparent;max-width:min(92vw,520px);width:520px;color:var(--rtx-text)}.rtx-dialog::backdrop{background:rgba(0,0,0,.72);backdrop-filter:blur(12px)}.rtx-dialog-card{border:1px solid var(--rtx-border);border-radius:26px;background:linear-gradient(145deg,color-mix(in srgb,var(--rtx-card) 94%,transparent),color-mix(in srgb,var(--rtx-soft) 92%,transparent));box-shadow:0 40px 110px rgba(0,0,0,.5);overflow:hidden}.rtx-dialog-head{display:flex;gap:14px;align-items:center;padding:22px;border-bottom:1px solid var(--rtx-border);background:linear-gradient(135deg,color-mix(in srgb,var(--rtx-accent) 15%,transparent),transparent)}.rtx-dialog-badge{width:48px;height:48px;border-radius:16px;display:grid;place-items:center;background:linear-gradient(135deg,#ffe28a,#d39b2a);color:#17130a;font-weight:650}.rtx-dialog h3{margin:0;font-size:23px;font-weight:650}.rtx-dialog p{margin:3px 0 0;color:var(--rtx-muted);font-weight:750}.rtx-dialog-body{padding:20px 22px}.rtx-dialog-actions{display:flex;justify-content:flex-end;gap:10px;padding:16px 22px;border-top:1px solid var(--rtx-border);background:rgba(0,0,0,.08)}.rtx-mini{height:44px;padding:0 18px;border-radius:15px;border:1px solid var(--rtx-border);background:var(--rtx-soft);color:var(--rtx-text);font-weight:650;cursor:pointer}.rtx-mini.primary{border:0;background:linear-gradient(135deg,#ffe28a,#d39b2a);color:#17130a}.rtx-checklist{margin:0;padding-left:20px;color:var(--rtx-muted);font-weight:750;line-height:1.7}.rtx-warn-text{color:color-mix(in srgb,var(--rtx-danger) 82%,var(--rtx-text))!important;font-weight:600!important;margin-top:12px!important}.rtx-checkbox{display:flex;align-items:center;gap:10px;color:var(--rtx-muted);font-weight:500;margin-top:14px}.rtx-checkbox input{width:18px;height:18px;accent-color:var(--rtx-accent)}.otp-field .rtx-input{height:56px;text-align:center;letter-spacing:8px;font-size:26px;font-weight:650}.rtx-otp-tools{display:flex;align-items:center;justify-content:space-between;gap:12px;color:var(--rtx-muted);font-weight:550}.rtx-link-btn{border:0;background:transparent;color:var(--rtx-accent);font-weight:650;cursor:pointer}.rtx-link-btn:disabled{opacity:.45;cursor:not-allowed}
  @media (prefers-reduced-motion:reduce){.rtx-auth *{animation:none!important;transition:none!important}}
  @media (max-width:980px){.rtx-auth-grid{grid-template-columns:1fr;align-items:start}.rtx-hero-panel{padding:28px}.rtx-benefits{grid-template-columns:1fr}.rtx-form-panel{max-width:620px;width:100%;margin:0 auto}.rtx-hero-panel h1{font-size:clamp(36px,9vw,56px)}}
  @media (max-width:560px){.rtx-auth{padding:14px;min-height:calc(100vh - 64px)}.rtx-auth-grid{gap:14px;min-height:auto}.rtx-hero-panel,.rtx-form-panel{border-radius:24px}.rtx-hero-panel{padding:24px 18px}.rtx-hero-panel p{font-size:15px}.rtx-benefits{margin-top:24px;gap:10px}.rtx-benefit{padding:14px;display:grid;grid-template-columns:42px 1fr;column-gap:12px;align-items:center}.rtx-benefit-icon{margin:0;width:40px;height:40px;grid-row:span 2}.rtx-proofline{font-size:11px}.rtx-form-panel{padding:22px 18px}.rtx-form-head{align-items:flex-start}.rtx-lock{width:48px;height:48px;border-radius:16px}.rtx-form-head h2{font-size:30px}.rtx-input-wrap{min-height:52px;grid-template-columns:48px minmax(0,1fr)}.rtx-password-wrap{grid-template-columns:48px minmax(0,1fr) 54px}.rtx-input{padding:0 12px}.rtx-pass{width:40px;height:40px;border-radius:14px}.rtx-submit{min-height:56px}.rtx-dialog{width:calc(100vw - 26px);max-width:calc(100vw - 26px)}.rtx-dialog-head{padding:18px}.rtx-dialog-body{padding:18px}.rtx-dialog-actions{padding:14px 18px}.rtx-mini{flex:1}.rtx-form-foot{justify-content:center;text-align:center}}
  @media (max-width:390px){.rtx-hero-panel h1{font-size:34px}.rtx-form-head h2{font-size:27px}.rtx-input-wrap{grid-template-columns:44px minmax(0,1fr)}.rtx-password-wrap{grid-template-columns:44px minmax(0,1fr) 50px}.rtx-input-icon{font-size:13px}.rtx-input{font-size:14px}.rtx-benefit{grid-template-columns:1fr}.rtx-benefit-icon{grid-row:auto;margin-bottom:10px}}

  /* =========================================================
     REGISTER NOTICE MODAL — ENTERPRISE HIGH-END
     แสดงทุกครั้ง ไม่มีตัวเลือกซ่อน 7 วัน
  ========================================================= */
  .rtx-enterprise-notice{
    --rn-card: color-mix(in srgb, var(--rtx-card,#17181d) 96%, #05060a 4%);
    --rn-card2: color-mix(in srgb, var(--rtx-soft,#101116) 92%, #1c2437 8%);
    --rn-text: var(--rtx-text,#fff6e3);
    --rn-muted: var(--rtx-muted,#d9c99d);
    --rn-border: color-mix(in srgb, var(--rtx-border,rgba(255,255,255,.12)) 80%, var(--rtx-accent,#f7cb3b) 20%);
    --rn-gold: var(--rtx-accent,#f7cb3b);
    --rn-gold2: var(--rtx-accent2,#d69a22);
    --rn-purple: #7c5cff;
    width:min(560px, calc(100vw - 24px)) !important;
    max-width:min(560px, calc(100vw - 24px)) !important;
    padding:0 !important;
    border:0 !important;
    background:transparent !important;
    overflow:visible !important;
    color:var(--rn-text);
  }

  .rtx-enterprise-notice::backdrop{
    background:
      radial-gradient(circle at 20% 15%, color-mix(in srgb, var(--rn-gold) 18%, transparent), transparent 34%),
      radial-gradient(circle at 85% 80%, rgba(124,92,255,.22), transparent 36%),
      rgba(0,0,0,.72);
    -webkit-backdrop-filter: blur(18px) saturate(135%);
    backdrop-filter: blur(18px) saturate(135%);
    animation:rtxNoticeBackdropIn .28s ease both;
  }

  .rtx-notice-card{
    position:relative;
    overflow:hidden;
    border-radius:30px;
    border:1px solid transparent;
    background:
      linear-gradient(145deg, var(--rn-card), var(--rn-card2)) padding-box,
      linear-gradient(135deg, color-mix(in srgb, var(--rn-gold) 72%, #fff 8%), rgba(124,92,255,.60), color-mix(in srgb, var(--rn-gold2) 72%, #000 8%)) border-box;
    box-shadow:
      0 34px 110px rgba(0,0,0,.58),
      0 0 0 1px rgba(255,255,255,.035),
      inset 0 1px 0 rgba(255,255,255,.10);
    animation:rtxNoticeIn .42s cubic-bezier(.2,.88,.2,1) both;
    isolation:isolate;
  }

  .rtx-enterprise-notice.is-closing .rtx-notice-card{
    animation:rtxNoticeOut .18s ease both;
  }

  .rtx-notice-card::before{
    content:"";
    position:absolute;
    inset:0 0 auto 0;
    height:4px;
    background:linear-gradient(90deg, #ffe48d, #c9962d, #7c5cff, #ffe48d);
    background-size:220% 100%;
    animation:rtxNoticeTopline 4.8s linear infinite;
    opacity:.95;
  }

  .rtx-notice-card::after{
    content:"";
    position:absolute;
    inset:-42% -30% auto auto;
    width:320px;
    height:320px;
    border-radius:999px;
    background:radial-gradient(circle, color-mix(in srgb, var(--rn-gold) 24%, transparent), transparent 66%);
    filter:blur(3px);
    pointer-events:none;
    z-index:-1;
    animation:rtxNoticeOrb 5.4s ease-in-out infinite;
  }

  .rtx-notice-aura{
    position:absolute;
    inset:auto auto -90px -85px;
    width:260px;
    height:260px;
    border-radius:999px;
    background:radial-gradient(circle, rgba(124,92,255,.20), transparent 68%);
    pointer-events:none;
    z-index:-1;
    animation:rtxNoticeOrb 6.2s ease-in-out infinite reverse;
  }

  .rtx-notice-head{
    position:relative;
    display:flex;
    gap:16px;
    align-items:center;
    padding:24px 24px 20px;
    border-bottom:1px solid rgba(255,255,255,.075);
    background:
      radial-gradient(circle at 12% 20%, color-mix(in srgb, var(--rn-gold) 18%, transparent), transparent 28%),
      linear-gradient(135deg, rgba(255,255,255,.08), rgba(255,255,255,.015));
  }

  .rtx-notice-badge{
    position:relative;
    width:62px;
    height:62px;
    flex:0 0 62px;
    border-radius:22px;
    display:grid;
    place-items:center;
    background:
      radial-gradient(circle at 30% 24%, rgba(255,255,255,.88), transparent 24%),
      linear-gradient(135deg, #ffe48d 0%, #d99e29 55%, #9c6a1a 100%);
    color:#17130a;
    box-shadow:
      0 18px 36px color-mix(in srgb, var(--rn-gold) 28%, transparent),
      inset 0 1px 0 rgba(255,255,255,.55),
      inset 0 -10px 18px rgba(78,43,5,.20);
    animation:rtxNoticeBadgeFloat 2.9s ease-in-out infinite;
  }

  .rtx-notice-badge::before{
    content:"";
    position:absolute;
    inset:-8px;
    border-radius:26px;
    border:1px solid color-mix(in srgb, var(--rn-gold) 35%, transparent);
    opacity:.72;
    animation:rtxNoticePulse 2.2s ease-in-out infinite;
  }

  .rtx-notice-badge span{
    font-size:28px;
    filter:drop-shadow(0 3px 8px rgba(0,0,0,.18));
  }

  .rtx-notice-kicker{
    display:inline-flex;
    margin-bottom:5px;
    color:color-mix(in srgb, var(--rn-gold) 88%, #fff 8%);
    font-size:11px;
    font-weight:950;
    letter-spacing:.12em;
    text-transform:uppercase;
  }

  .rtx-notice-titlebox h3{
    margin:0;
    font-size:clamp(22px, 3vw, 30px);
    line-height:1.15;
    letter-spacing:-.035em;
    font-weight:950;
    color:var(--rn-text);
  }

  .rtx-notice-titlebox p{
    margin:6px 0 0;
    color:color-mix(in srgb, var(--rn-muted) 88%, var(--rn-text) 12%);
    font-weight:750;
    line-height:1.45;
  }

  .rtx-notice-body{
    padding:20px 22px 6px;
  }

  .rtx-notice-list{
    display:grid;
    gap:12px;
  }

  .rtx-notice-item{
    display:grid;
    grid-template-columns:46px minmax(0, 1fr);
    gap:13px;
    align-items:start;
    padding:14px;
    border-radius:20px;
    border:1px solid rgba(255,255,255,.075);
    background:
      linear-gradient(135deg, rgba(255,255,255,.055), rgba(255,255,255,.018));
    box-shadow:inset 0 1px 0 rgba(255,255,255,.055);
    transform:translateY(8px);
    opacity:0;
    animation:rtxNoticeItemIn .42s cubic-bezier(.2,.8,.2,1) forwards;
  }

  .rtx-notice-item:nth-child(2){ animation-delay:.08s; }
  .rtx-notice-item:nth-child(3){ animation-delay:.16s; }

  .rtx-notice-item.warn{
    border-color:color-mix(in srgb, var(--rn-gold) 34%, rgba(255,255,255,.075));
    background:
      radial-gradient(circle at 8% 12%, color-mix(in srgb, var(--rn-gold) 16%, transparent), transparent 44%),
      linear-gradient(135deg, rgba(255,255,255,.058), rgba(255,255,255,.018));
  }

  .rtx-notice-no{
    width:42px;
    height:42px;
    border-radius:16px;
    display:grid;
    place-items:center;
    color:#17130a;
    background:linear-gradient(135deg, #ffe48d, #d39b2a);
    font-size:13px;
    font-weight:950;
    box-shadow:0 12px 24px color-mix(in srgb, var(--rn-gold) 20%, transparent);
  }

  .rtx-notice-item strong{
    display:block;
    color:var(--rn-text);
    font-weight:950;
    margin:1px 0 4px;
  }

  .rtx-notice-item small{
    display:block;
    color:color-mix(in srgb, var(--rn-muted) 88%, transparent);
    line-height:1.55;
    font-weight:700;
  }

  .rtx-notice-warning{
    margin-top:14px;
    display:flex;
    align-items:flex-start;
    gap:10px;
    padding:13px 14px;
    border-radius:18px;
    border:1px solid color-mix(in srgb, var(--rn-gold) 24%, rgba(255,255,255,.08));
    background:color-mix(in srgb, var(--rn-gold) 8%, transparent);
  }

  .rtx-notice-warning span{
    color:var(--rn-gold);
    margin-top:1px;
    text-shadow:0 0 18px color-mix(in srgb, var(--rn-gold) 62%, transparent);
  }

  .rtx-notice-warning p{
    margin:0;
    color:color-mix(in srgb, var(--rn-muted) 92%, var(--rn-text) 8%);
    line-height:1.55;
    font-weight:800;
  }

  .rtx-notice-actions{
    display:flex;
    justify-content:flex-end;
    padding:16px 22px 22px;
  }

  .rtx-notice-accept{
    position:relative;
    overflow:hidden;
    min-height:48px;
    border:0;
    border-radius:999px;
    padding:0 18px 0 22px;
    display:inline-flex;
    align-items:center;
    justify-content:center;
    gap:10px;
    cursor:pointer;
    color:#17130a;
    background:linear-gradient(135deg, #ffe48d 0%, #e6b54a 48%, #bd8626 100%);
    font-weight:950;
    box-shadow:
      0 18px 38px color-mix(in srgb, var(--rn-gold) 26%, transparent),
      inset 0 1px 0 rgba(255,255,255,.58);
    transition:transform .2s ease, filter .2s ease, box-shadow .2s ease;
  }

  .rtx-notice-accept:hover{
    transform:translateY(-2px);
    filter:saturate(1.08);
    box-shadow:
      0 23px 45px color-mix(in srgb, var(--rn-gold) 34%, transparent),
      inset 0 1px 0 rgba(255,255,255,.62);
  }

  .rtx-notice-accept .shine{
    position:absolute;
    inset:-70% auto -70% -42%;
    width:42%;
    transform:rotate(18deg);
    background:linear-gradient(90deg, transparent, rgba(255,255,255,.55), transparent);
    animation:rtxNoticeShine 3.6s ease-in-out infinite;
    pointer-events:none;
  }

  .rtx-notice-accept b{
    display:grid;
    place-items:center;
    width:22px;
    height:22px;
    border-radius:999px;
    background:rgba(0,0,0,.13);
    font-size:15px;
  }

  @keyframes rtxNoticeBackdropIn{ from{opacity:0;} to{opacity:1;} }
  @keyframes rtxNoticeIn{ from{opacity:0; transform:translateY(16px) scale(.955); filter:blur(8px);} to{opacity:1; transform:translateY(0) scale(1); filter:blur(0);} }
  @keyframes rtxNoticeOut{ from{opacity:1; transform:translateY(0) scale(1); filter:blur(0);} to{opacity:0; transform:translateY(10px) scale(.97); filter:blur(5px);} }
  @keyframes rtxNoticeTopline{ from{background-position:0 0;} to{background-position:220% 0;} }
  @keyframes rtxNoticeOrb{ 0%,100%{transform:translate3d(0,0,0) scale(1); opacity:.85;} 50%{transform:translate3d(-12px,12px,0) scale(1.08); opacity:.55;} }
  @keyframes rtxNoticeBadgeFloat{ 0%,100%{transform:translateY(0) rotate(0deg);} 50%{transform:translateY(-4px) rotate(-1deg);} }
  @keyframes rtxNoticePulse{ 0%,100%{transform:scale(.94); opacity:.35;} 50%{transform:scale(1.08); opacity:.86;} }
  @keyframes rtxNoticeItemIn{ to{opacity:1; transform:translateY(0);} }
  @keyframes rtxNoticeShine{ 0%,45%{left:-48%;} 72%,100%{left:122%;} }

  @media (max-width:560px){
    .rtx-enterprise-notice{
      width:calc(100vw - 22px) !important;
      max-width:calc(100vw - 22px) !important;
    }
    .rtx-notice-card{ border-radius:26px; }
    .rtx-notice-head{
      padding:20px 18px 16px;
      align-items:flex-start;
    }
    .rtx-notice-badge{
      width:54px;
      height:54px;
      flex-basis:54px;
      border-radius:19px;
    }
    .rtx-notice-body{ padding:16px 16px 4px; }
    .rtx-notice-item{
      grid-template-columns:40px minmax(0,1fr);
      padding:12px;
      border-radius:18px;
    }
    .rtx-notice-no{
      width:38px;
      height:38px;
      border-radius:14px;
      font-size:12px;
    }
    .rtx-notice-actions{
      padding:14px 16px 18px;
    }
    .rtx-notice-accept{
      width:100%;
    }
  }

  @media (prefers-reduced-motion:reduce){
    .rtx-enterprise-notice *,
    .rtx-enterprise-notice::backdrop{
      animation:none !important;
      transition:none !important;
    }
  }

  .rtx-meta{margin-top:auto;padding-top:36px;display:flex;align-items:center;flex-wrap:wrap;gap:10px;color:color-mix(in srgb,var(--rtx-accent) 76%,var(--rtx-muted));font-size:12px;font-weight:900;}
  .rtx-meta i{width:5px;height:5px;border-radius:50%;background:var(--rtx-accent);opacity:.76;}
  

</style>

<script>
(() => {
  const modal = document.getElementById('registerNoticeModal');
  const closeBtn = document.getElementById('regNoticeClose');

  function closeNotice(){
    if (!modal) return;
    modal.classList.add('is-closing');
    window.setTimeout(() => {
      try { if (modal.open) modal.close(); } catch(e){}
      modal.classList.remove('is-closing');
    }, 190);
  }

  document.addEventListener('DOMContentLoaded', () => {
    requestAnimationFrame(() => {
      window.setTimeout(() => {
        try {
          if (modal && !modal.open) modal.showModal();
        } catch(e) {}
      }, 120);
    });
  });

  closeBtn?.addEventListener('click', closeNotice);
  modal?.addEventListener('cancel', (ev) => {
    ev.preventDefault();
    closeNotice();
  });
})();
</script>

<script type="module">
const notify = ({ variant='info', title='ข้อความ', text='', timeout } = {}) => {
  if (window.notify && typeof window.notify.push === 'function') {
    window.notify.push({ variant, title, text, timeout });
  } else if (typeof window.notify === 'function') {
    window.notify({ variant, title, text, timeout });
  } else {
    alert((title ? title + '\n' : '') + (text || ''));
  }
};

const $ = (s, p=document)=>p.querySelector(s);
const form = $('#registerForm');
const btnRegister = $('#btnRegister');

const msgDlg = $('#msgDialog');
const msgBadge = $('#msgBadge');
const msgTitle = $('#msgTitle');
const msgText = $('#msgText');
const msgClose = $('#msgClose');
const msgCancel = $('#msgCancel');

function showMsg({ variant='info', icon, title='ข้อความ', text='', okText='ตกลง', cancelText, onOk, onCancel } = {}) {
  const iconMap = { info:'❔', success:'✅', warn:'⚠️', error:'✖' };
  msgDlg?.setAttribute('data-variant', variant);
  if (msgBadge) msgBadge.textContent = icon || iconMap[variant] || '❔';
  if (msgTitle) msgTitle.textContent = title;
  if (msgText) msgText.textContent = text;
  if (msgClose) msgClose.textContent = okText;
  if (msgCancel) {
    if (cancelText) { msgCancel.hidden = false; msgCancel.textContent = cancelText; }
    else msgCancel.hidden = true;
  }
  if (msgClose) msgClose.onclick = ()=>{ msgDlg.close(); onOk?.(); };
  if (msgCancel) msgCancel.onclick = ()=>{ msgDlg.close(); onCancel?.(); };
  if (msgDlg && !msgDlg.open) msgDlg.showModal();
}
msgDlg?.addEventListener('cancel', e=>{ e.preventDefault(); msgDlg.close(); });

const __flash = (typeof window.__flash !== 'undefined' ? window.__flash : null);
if (__flash) {
  showMsg(__flash);
  window.__flash = null;
}

const pwd = $('#pwd');
const togglePwd = $('#togglePwd');
togglePwd?.addEventListener('click', ()=>{
  const show = pwd.type === 'password';
  pwd.type = show ? 'text' : 'password';
  togglePwd.setAttribute('aria-pressed', String(show));
  togglePwd.classList.toggle('is-visible', show);
});

let nextUrl = '/';
form?.addEventListener('submit', async (e)=>{
  e.preventDefault();
  btnRegister.disabled = true;

  const fd = new FormData(form);
  nextUrl = fd.get('next') || '/';

  const payload = new URLSearchParams();
  ['username','email','name','password','next','aff','affiliateKey'].forEach(k=>{
    const v = fd.get(k);
    if (v != null) payload.append(k, v);
  });

  try {
    const res = await fetch('/register', {
      method: 'POST',
      headers: { 'Accept':'application/json', 'Content-Type':'application/x-www-form-urlencoded' },
      body: payload
    });

    let data = {};
    try { data = await res.json(); } catch {}

    if (!res.ok || !data?.ok) {
      notify({ variant:'error', title:'สมัครสมาชิกไม่สำเร็จ', text: data?.message || 'กรุณาตรวจสอบข้อมูล' });
      return;
    }

    if (data.needOtp) {
      openOtp(data.email, data.ttl || 300, 60);
      return;
    }

    notify({ variant:'success', title:'ส่งอีเมลยืนยันแล้ว', text:'โปรดตรวจกล่องจดหมายของคุณ แล้วคลิกลิงก์เพื่อเปิดใช้งานบัญชี' });
  } catch (err) {
    notify({ variant:'error', title:'เครือข่ายมีปัญหา', text:'โปรดลองใหม่อีกครั้ง' });
  } finally {
    btnRegister.disabled = false;
  }
});

const otpDlg = $('#otpDialog');
const otpCode = $('#otpCode');
const btnOtpOK = $('#btnVerifyOtp');
const btnOtpCancel = $('#btnCancelOtp');
const btnResend = $('#btnResend');
const otpTimer = $('#otpTimer');
const otpHelp = $('#otpHelp');
let pendingEmail = '';
let timerId = null;

function openOtp(email, ttl=300, cooldown=60){
  pendingEmail = email;
  if (otpHelp) otpHelp.textContent = `เราได้ส่งรหัส 6 หลักไปที่ ${email}`;
  if (otpCode) otpCode.value = '';
  if (btnResend) btnResend.disabled = true;
  startTimer(cooldown);
  if (otpDlg && !otpDlg.open) otpDlg.showModal();
  otpCode?.focus();
}
function closeOtp(){
  clearInterval(timerId); timerId = null;
  if (otpDlg?.open) otpDlg.close();
}
function startTimer(sec){
  clearInterval(timerId);
  let left = sec;
  if (otpTimer) otpTimer.textContent = `ส่งใหม่ได้ใน ${left}s`;
  timerId = setInterval(()=>{
    left--;
    if (left <= 0) {
      clearInterval(timerId); timerId = null;
      if (otpTimer) otpTimer.textContent = 'ส่งใหม่ได้ตอนนี้';
      if (btnResend) btnResend.disabled = false;
    } else if (otpTimer) {
      otpTimer.textContent = `ส่งใหม่ได้ใน ${left}s`;
    }
  },1000);
}

btnOtpOK?.addEventListener('click', async ()=>{
  const code = (otpCode.value || '').trim();
  if (!/^\d{6}$/.test(code)) {
    notify({ variant:'warn', title:'รหัสไม่ครบ', text:'กรอกรหัส OTP 6 หลักให้ถูกต้อง' });
    return;
  }
  try {
    const verifyRes = await fetch('/otp/verify', {
      method: 'POST',
      headers: { 'Content-Type':'application/x-www-form-urlencoded', 'Accept':'application/json' },
      body: new URLSearchParams({ email: pendingEmail, code })
    });
    const vr = await verifyRes.json().catch(()=> ({}));
    if (!verifyRes.ok || !vr?.ok) {
      notify({ variant:'error', title:'ยืนยันไม่สำเร็จ', text: vr?.error || 'รหัสไม่ถูกต้อง' });
      return;
    }
    const finRes = await fetch('/register/finalize', {
      method: 'POST',
      headers: { 'Content-Type':'application/x-www-form-urlencoded', 'Accept':'application/json' },
      body: new URLSearchParams({ next: nextUrl })
    });
    const fr = await finRes.json().catch(()=> ({}));
    if (!finRes.ok || !fr?.ok) {
      notify({ variant:'error', title:'สร้างบัญชีไม่ได้', text: fr?.message || 'โปรดลองใหม่' });
      return;
    }
    closeOtp();
    notify({ variant:'success', title:'สมัครสำเร็จ!', text:'ล็อกอินสำเร็จ' });
    setTimeout(()=> location.assign(fr.redirect || nextUrl || '/'), 500);
  } catch(e) {
    notify({ variant:'error', title:'เครือข่ายมีปัญหา', text:'โปรดลองใหม่อีกครั้ง' });
  }
});

btnResend?.addEventListener('click', async ()=>{
  btnResend.disabled = true;
  try {
    const rs = await fetch('/otp/send', {
      method: 'POST',
      headers: { 'Content-Type':'application/x-www-form-urlencoded', 'Accept':'application/json' },
      body: new URLSearchParams({ email: pendingEmail })
    });
    const data = await rs.json().catch(()=> ({}));
    if (!rs.ok || !data?.ok) {
      notify({ variant:'error', title:'ส่งรหัสไม่สำเร็จ', text: data?.error || 'โปรดลองใหม่' });
      btnResend.disabled = false;
      return;
    }
    startTimer(60);
    notify({ variant:'success', title:'ส่งรหัสใหม่แล้ว', text:'โปรดตรวจที่อีเมลของคุณ' });
  } catch(e) {
    notify({ variant:'error', title:'ส่งรหัสไม่สำเร็จ', text:'โปรดลองใหม่' });
    btnResend.disabled = false;
  }
});

btnOtpCancel?.addEventListener('click', closeOtp);
otpCode?.addEventListener('input', ()=> { otpCode.value = otpCode.value.replace(/[^\d]/g,'').slice(0,6); });
otpCode?.addEventListener('keydown', (e)=>{ if (e.key === 'Enter') btnOtpOK?.click(); });
</script>

<script>
(() => {
  const url = new URL(location.href);
  const fromAff = url.searchParams.get('aff');
  const weird = (!fromAff && url.search.startsWith('?=')) ? url.search.slice(2) : '';
  const key = (fromAff || weird || '').trim();

  if (key) {
    if (!document.cookie.split('; ').some(c => c.startsWith('affiliate_ref='))) {
      const maxAge = 30 * 24 * 60 * 60;
      document.cookie = `affiliate_ref=${encodeURIComponent(key)}; Path=/; Max-Age=${maxAge}; SameSite=Lax`;
    }
    url.searchParams.delete('aff');
    if (weird) history.replaceState(null, '', url.pathname + url.hash);
    else history.replaceState(null, '', url.pathname + url.search + url.hash);
  }
})();
</script>
